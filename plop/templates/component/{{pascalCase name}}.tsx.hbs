{{#if forward}}
/*=============================================== {{>pascalName }} component ===============================================*/

import { forwardRef } from "react"
import { Styled{{>pascalName }} } from "./styles"
import type { I{{>pascalName }} } from "./types"

export const {{>pascalName }} = forwardRef<HTML{{ pascalCase attribute }}Element, I{{>pascalName }}>(({ }, ref) => {
    return (
        <Styled{{>pascalName }} ref={ref}>
            
        </Styled{{>pascalName }}>
    )
})
{{else}}
/*=============================================== {{>pascalName }} component ===============================================*/

import { Styled{{>pascalName }} } from "./styles"
import type { I{{>pascalName }} } from "./types"

export const {{>pascalName }}: FC<I{{>pascalName }}> = ({ }) => {
    return (
        <Styled{{>pascalName }}>
            
        </Styled{{>pascalName }}>
    )
}
{{/if}}