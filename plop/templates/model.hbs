/*=============================================== {{>pascalName }} model ===============================================*/

import { Schema, model } from "mongoose"
import type { I{{>pascalName }} } from "../../shared/types"

const {{ camelCase name }}Schema = new Schema<I{{>pascalName }}>(
    {
        
    },
    { timestamps: true }
)

export const {{>pascalName }}Model = model<I{{>pascalName }}>("{{>pascalName }}", {{ camelCase name }}Schema)
